import React from 'react';
import { Text } from 'react-native';

export default function AppConfiguration () {
  return <Text>Global configuration page (another configuration page incoming to edit things</Text>
}